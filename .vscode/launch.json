{
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome on localhost:8080",
            "url": "http://localhost:8080",
            "webRoot": "${workspaceFolder}"
        }
    ]
}